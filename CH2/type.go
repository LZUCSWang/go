package main

// import (
// 	"fmt"
//
//
// )

type Celsius float64
type Fahrenheit float64

const (
	AbsoluteZeroC Celsius = -273.15
	FreezingC     Celsius = 0
	BoilingC      Celsius = 100
)

// 导入包的内容太复杂先跳过
func main() {
	// var c Celsius
	// var f Fahrenheit
	// fmt.Println(c == 0, f >= 0, c == tempconv.Celsius(f), c == Celsius(f))

	// // fmt.Println(c == f)// 不同的类型不能直接比较
	// cc := tempconv.fToC(212.0)
	// fmt.Println(cc)
}
